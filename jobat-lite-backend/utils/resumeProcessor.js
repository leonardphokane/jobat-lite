module.exports = async (buffer) => {
  const text = buffer.toString(); // Simplified for now
  return {
    name: 'Lebo Mokoena',
    skills: ['React', 'Node.js', 'MongoDB'],
    experience: '3 years'
  };
};
